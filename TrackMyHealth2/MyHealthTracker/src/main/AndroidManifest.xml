<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="myhealth.october.com.trackmyhealth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main_Menu" />
        <activity android:name=".blood_pressure" />
        <activity android:name=".read_blood_pressure" />
        <activity android:name=".blood_sugar" />
        <activity android:name=".weight" />
        <activity android:name=".meds" />
        <activity android:name=".write_blood_pressure"></activity>
    </application>

</manifest>